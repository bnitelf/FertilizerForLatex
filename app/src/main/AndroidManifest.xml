<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.puicbr.fertilizerforlatex" >

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivityOld"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StartActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SelectDateActivity" />
        <!--<activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->
        <activity android:name=".Sub_main" />
        <activity android:name=".TaskListActivity" />
        <activity android:name=".TaskDetailActivity" />
        <activity
            android:name=".TaskEditActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FertilizingRoundActivity"
            android:label="@string/title_activity_fertilizing_round" >
        </activity>
        <activity
            android:name=".FertilizingGuideActivity"
            android:label="@string/title_activity_fertilizing_guide" >
        </activity>

        <receiver android:name=".BroadcastReceiver.AlarmReceiver"></receiver>
    </application>

</manifest>
